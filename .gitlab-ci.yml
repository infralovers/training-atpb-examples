image: "python:3.8"

before_script:
  - python --version
  - pip install -r requirements.txt

stages:
  - linting
  - behave
#defines the job in Static Analysis
pylint:
  stage: linting
  script:
  - pylint *.py

behave:
  stage: behave
  script:
  - echo "hello"